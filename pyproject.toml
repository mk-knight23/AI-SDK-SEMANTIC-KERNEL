[project]
name = "ai_sdk_semantic_kernel"
version = "0.2.0"
description = "Kazi AI SDK ecosystem adapter"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
